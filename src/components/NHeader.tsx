import { NLogo } from '.'

export const NHeader = () => (
  <header className="bg-gray-700 min-h-[200px] flex items-center justify-center">
    <h1 aria-label="Rocket To-do">
      <NLogo />
    </h1>
  </header>
)
